% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_stat.R, R/scale01.R
\name{scale01}
\alias{scale01}
\title{Scale values to 0-1 range}
\usage{
scale01(x)

scale01(x)
}
\arguments{
\item{x}{A numeric vector to be scaled. Missing values (NA) are allowed.}
}
\value{
A numeric vector of the same length as input, scaled to [0, 1] range.
        If all values in x are identical, returns a vector of zeros.
        Missing values in input will result in missing values in output.

A numeric vector of the same length as input, scaled to [0, 1] range.
        If all values in x are identical, returns a vector of zeros.
        Missing values in input will result in missing values in output.
}
\description{
This function scales a numeric vector to the range [0, 1] using min-max normalization.
The transformation is: (x - min(x)) / (max(x) - min(x))

This function scales a numeric vector to the range [0, 1] using min-max normalization.
The transformation is: (x - min(x)) / (max(x) - min(x))
}
\details{
This function performs min-max normalization, which linearly transforms 
the data to fit within the range [0, 1]. The minimum value becomes 0 
and the maximum value becomes 1.

This function performs min-max normalization, which linearly transforms 
the data to fit within the range [0, 1]. The minimum value becomes 0 
and the maximum value becomes 1.
}
\examples{
# Basic usage
scale01(c(1, 2, 3, 4, 5))

# With negative numbers
scale01(c(-2, 0, 2, 4))

# With identical values
scale01(c(5, 5, 5))

# With missing values
scale01(c(1, 2, NA, 4, 5))

# Basic usage
scale01(c(1, 2, 3, 4, 5))

# With negative numbers
scale01(c(-2, 0, 2, 4))

# With identical values
scale01(c(5, 5, 5))

# With missing values
scale01(c(1, 2, NA, 4, 5))

}
\seealso{
\code{\link{scale}} for standardization (z-score normalization)

\code{\link{scale}} for standardization (z-score normalization)
}
\author{
Xiang LI \email{lixiang117423@foxmail.com}
}
