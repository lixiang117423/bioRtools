% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_str.R
\name{filter_str}
\alias{filter_str}
\title{Filter rows by matching a string/regex pattern in a column}
\usage{
filter_str(data, col, pattern, negate = FALSE)
}
\arguments{
\item{data}{A data frame.}

\item{col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Bare column
name to search, e.g. \code{kegg_term}.}

\item{pattern}{Regex pattern to match against the column.}

\item{negate}{Logical; if \code{TRUE}, keep rows that do NOT match.}
}
\value{
\code{data} filtered to matching rows. Rows where \code{col} is
  \code{NA} are dropped, matching \code{dplyr::filter()} semantics.
}
\description{
Convenience wrapper around \code{dplyr::filter(stringr::str_detect(...))}.
Keeps rows where \code{col} matches \code{pattern}; with \code{negate = TRUE}
it keeps the rows that do NOT match (the \code{!str_detect(...)} case).
}
\examples{
\dontrun{
df <- data.frame(name = c("MAPK1", "MAPK3", "AKT1", "mTOR"))

# keep rows whose name contains "MAPK"
filter_str(df, name, "MAPK")

# keep rows whose name does NOT contain "MAPK"
filter_str(df, name, "MAPK", negate = TRUE)
}
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
