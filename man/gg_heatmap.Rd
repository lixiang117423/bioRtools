% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_heatmap.R
\name{gg_heatmap}
\alias{gg_heatmap}
\title{ggplot2-based Heatmap}
\usage{
gg_heatmap(
  mat,
  color = (grDevices::colorRampPalette(c("#0C6291", "white", "#A63446")))(100),
  breaks = NULL,
  scale = c("none", "row", "column"),
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  clustering_distance_rows = "euclidean",
  clustering_distance_cols = "euclidean",
  clustering_method = "complete",
  cutree_rows = NA,
  cutree_cols = NA,
  treeheight_row = 1,
  treeheight_col = 1,
  show_rownames = TRUE,
  show_colnames = TRUE,
  annotation_row = NULL,
  annotation_col = NULL,
  annotation_colors = NULL,
  annotation_legend = TRUE,
  main = NULL,
  fontsize = 10,
  fontsize_row = fontsize,
  fontsize_col = fontsize,
  cellwidth = NULL,
  cellheight = NULL,
  angle_col = 270,
  display_numbers = FALSE,
  number_format = "\%.2f",
  number_color = "grey30",
  gaps_row = NULL,
  gaps_col = NULL,
  border_color = "grey60",
  na_col = "#DDDDDD",
  legend = TRUE,
  filename = NULL,
  width = 7,
  height = 7,
  silent = FALSE
)
}
\arguments{
\item{mat}{Matrix to plot as a heatmap.}

\item{color}{Color palette as a character vector.
Default is a 100-color gradient from blue to white to red.}

\item{breaks}{Numeric vector of breaks for the color scale. If NULL,
automatically generated from the data range.}

\item{scale}{One of "none", "row", or "column". Whether to scale values
before plotting. Default is "none".}

\item{cluster_rows}{Logical or hclust object. If TRUE, cluster rows using
hierarchical clustering. If an hclust object, use it directly.}

\item{cluster_cols}{Logical or hclust object. Same as cluster_rows but for
columns.}

\item{clustering_distance_rows}{Distance method for row clustering.
Default is "euclidean".}

\item{clustering_distance_cols}{Distance method for column clustering.
Default is "euclidean".}

\item{clustering_method}{Agglomeration method for hierarchical clustering.
Default is "complete".}

\item{cutree_rows}{Number of row clusters to create via cutree, generating
gap lines. Default is NA (no cutting).}

\item{cutree_cols}{Number of column clusters to create via cutree.
Default is NA (no cutting).}

\item{treeheight_row}{Relative height of the row dendrogram in the layout.
Default is 1.}

\item{treeheight_col}{Relative height of the column dendrogram in the layout.
Default is 1.}

\item{show_rownames}{Logical, whether to display row names. Default is TRUE.}

\item{show_colnames}{Logical, whether to display column names. Default is TRUE.}

\item{annotation_row}{Data frame for row annotations. Row names must match
\code{mat} row names.}

\item{annotation_col}{Data frame for column annotations. Row names must match
\code{mat} column names.}

\item{annotation_colors}{Named list of color vectors for annotation variables.
Names must match column names in annotation_row/annotation_col.}

\item{annotation_legend}{Logical, whether to show annotation legends.
Default is TRUE.}

\item{main}{Character string for the plot title. Default is NULL.}

\item{fontsize}{Base font size in points. Default is 10.}

\item{fontsize_row}{Font size for row labels. Default is fontsize.}

\item{fontsize_col}{Font size for column labels. Default is fontsize.}

\item{cellwidth}{Fixed cell width in points. Default is NULL (auto).}

\item{cellheight}{Fixed cell height in points. Default is NULL (auto).}

\item{angle_col}{Column label rotation angle. Default is 270.}

\item{display_numbers}{Logical or matrix. If TRUE, display cell values.
If a matrix, display those values instead.}

\item{number_format}{Format string for cell numbers. Default is "\%.2f".}

\item{number_color}{Color for cell number text. Default is "grey30".}

\item{gaps_row}{Integer vector of row positions for gap lines.}

\item{gaps_col}{Integer vector of column positions for gap lines.}

\item{border_color}{Cell border color. Default is "grey60".}

\item{na_col}{Color for NA values. Default is "\#DDDDDD".}

\item{legend}{Logical, whether to show the color legend. Default is TRUE.}

\item{filename}{File path to save the plot. Default is NULL (no saving).}

\item{width}{Plot width in inches for saving. Default is 7.}

\item{height}{Plot height in inches for saving. Default is 7.}

\item{silent}{Logical, whether to suppress plot display. Default is FALSE.}
}
\value{
Invisibly returns a list containing:
  \describe{
    \item{plot}{The patchwork ggplot object}
    \item{hc_row}{Row hclust object (or NULL)}
    \item{hc_col}{Column hclust object (or NULL)}
    \item{row_ord}{Row order after clustering}
    \item{col_ord}{Column order after clustering}
    \item{mat_ord}{The ordered matrix}
  }
}
\description{
A ggplot2 rewrite of \code{pheatmap::pheatmap()} with a similar interface.
Supports hierarchical clustering, dendrograms, row/column annotations,
gap lines, and most common heatmap features.
}
\examples{
set.seed(42)
mat <- matrix(rnorm(60), nrow = 6,
  dimnames = list(paste0("Gene", 1:6), paste0("Sample", 1:10)))

# Basic usage
if (interactive()) {
  bioRtools::gg_heatmap(mat)
}
}
