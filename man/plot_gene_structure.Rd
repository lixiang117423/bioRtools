% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gene_structure.R
\name{plot_gene_structure}
\alias{plot_gene_structure}
\title{Plot Gene Structure from GFF/GFF3 File}
\usage{
plot_gene_structure(
  gff,
  tree = "none",
  exon_color = "#4DAF4A",
  cds_color = "#377EB8",
  utr_color = "#FF7F00",
  gene_line_size = 0.8,
  gene_height = 0.4,
  x_label = "",
  base_size = 12
)
}
\arguments{
\item{gff}{Path to GFF/GFF3 file containing gene annotation data}

\item{tree}{Either "none" (default) or path to a Newick tree file for
phylogenetic ordering of genes}

\item{exon_color}{Color for exon features (default: "#4DAF4A")}

\item{cds_color}{Color for CDS features (default: "#377EB8")}

\item{utr_color}{Color for UTR features (default: "#FF7F00")}

\item{gene_line_size}{Size of gene line (default: 0.8)}

\item{gene_height}{Height of gene features (default: 0.4)}

\item{x_label}{X-axis label (default: "")}

\item{base_size}{Base font size for the plot (default: 12)}
}
\value{
A ggplot object displaying gene structure with:
  - Horizontal lines representing gene spans
  - Rectangles showing exons, CDS, and UTR regions
  - Color-coded feature types (exon, CDS, UTR)
  - Optional phylogenetic tree layout
}
\description{
Visualize gene structure including exons, introns, and other genomic features
from GFF/GFF3 format files. This function parses gene annotation files and
creates publication-ready gene structure plots with optional phylogenetic tree.
}
\examples{
\dontrun{
library(bioRtools)

# Basic usage without tree
gff_file <- system.file("extdata", "genes.gff3", package = "bioRtools")
p1 <- plot_gene_structure(gff = gff_file)
print(p1)

# With phylogenetic tree
tree_file <- system.file("extdata", "gene_tree.nwk", package = "bioRtools")
p2 <- plot_gene_structure(
  gff = gff_file,
  tree = tree_file,
  exon_color = "#E41A1C",
  cds_color = "#377EB8"
)
print(p2)

# Save plot
ggplot2::ggsave("gene_structure.pdf", p1, width = 10, height = 6)
}
}
