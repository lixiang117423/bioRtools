% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairs_by_group.R
\name{pairs_by_group}
\alias{pairs_by_group}
\title{Generate All Pairs Within Each Group}
\usage{
pairs_by_group(df, group_col, id_col, out_names = c("Gene1", "Gene2"))
}
\arguments{
\item{df}{A data frame.}

\item{group_col}{Column to group by (bare name or character string).}

\item{id_col}{Column containing IDs to pair (bare name or character string).}

\item{out_names}{Character vector of length 2 naming the two output pair
columns (default: \code{c("Gene1", "Gene2")}).}
}
\value{
A tibble with columns: \code{group_col}, \code{out_names[1]},
  \code{out_names[2]}. Groups with fewer than 2 IDs are silently dropped.
}
\description{
For each group in a data frame, generate all \eqn{C(n, 2)} pairwise
combinations of values in a specified ID column. Useful for KaKs
calculation between gene pairs within clades, co-evolution analysis,
or within-group network construction.
}
\examples{
\dontrun{
df <- data.frame(
  type = c("A", "A", "A", "B", "B"),
  gene = c("g1", "g2", "g3", "g4", "g5")
)
pairs_by_group(df, type, gene)
# # A tibble: 4 x 3
#   type  Gene1 Gene2
#   <chr> <chr> <chr>
# 1 A     g1    g2
# 2 A     g1    g3
# 3 A     g2    g3
# 4 B     g4    g5

# Original inline pattern this replaces:
# df \%>\% group_by(type) \%>\% group_modify(~ { ... combn(ids, 2) ... })
}

}
\author{
Xiang LI <lixiang117423@gmail.com>
}
