% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dual_axis.R
\name{plot_dual_axis}
\alias{plot_dual_axis}
\title{Create a dual Y-axis plot with area and scatter layers}
\usage{
plot_dual_axis(
  data,
  x,
  area_col = NULL,
  scatter_cols = NULL,
  left_range = NULL,
  right_range = NULL,
  phases = NULL,
  area_fill = "#bad6f980",
  area_fill_name = NULL,
  area_border_color = "grey70",
  scatter_colors = NULL,
  scatter_shapes = NULL,
  left_label = NULL,
  right_label = NULL,
  x_label = NULL,
  x_breaks = NULL,
  left_breaks = NULL,
  right_breaks = NULL,
  point_size = 2.5,
  legend_position = "top",
  base_size = 11
)
}
\arguments{
\item{data}{A data frame in wide format containing the plotting data.}

\item{x}{Character string specifying the column name for the x-axis.}

\item{area_col}{Character string specifying the column name for the area chart,
mapped to the right Y-axis. Default is NULL.}

\item{scatter_cols}{Character vector of column names for scatter points,
mapped to the left Y-axis. Default is NULL.}

\item{left_range}{Numeric vector of length 2 for left Y-axis range (c(min, max)).
Auto-calculated from scatter data if NULL.}

\item{right_range}{Numeric vector of length 2 for right Y-axis range (c(min, max)).
Auto-calculated from area data if NULL.}

\item{phases}{Optional data frame with columns: \code{start}, \code{end},
\code{label}, and optionally \code{fill_color}, \code{border_color}.
Adds colored rectangles at the top of the plot to indicate phases.}

\item{area_fill}{Character string for area fill color (supports 8-digit hex
with alpha, e.g., "#bad6f980"). Default is "#bad6f980".}

\item{area_fill_name}{Character string for the area legend label.
Default uses the area column name.}

\item{area_border_color}{Character string for area border color.
Default is "grey70".}

\item{scatter_colors}{Named character vector of colors for scatter series.
Default uses RColorBrewer "Set1" palette.}

\item{scatter_shapes}{Named integer vector of shapes for scatter series.
Default uses sequential integers starting from 1.}

\item{left_label}{Character string for left Y-axis label. Default is "Value".}

\item{right_label}{Character string for right Y-axis label.
Default uses the area column name.}

\item{x_label}{Character string for x-axis label. Default uses the x column name.}

\item{x_breaks}{Numeric vector of breaks for the x-axis. Default is NULL (auto).}

\item{left_breaks}{Numeric vector of breaks for the left Y-axis. Default is NULL (auto).}

\item{right_breaks}{Numeric vector of breaks for the right Y-axis. Default is NULL (auto).}

\item{point_size}{Numeric value for scatter point size. Default is 2.5.}

\item{legend_position}{Character string or numeric vector for legend position.
Default is "top".}

\item{base_size}{Numeric value for base font size. Default is 11.}
}
\value{
A ggplot object.
}
\description{
Create a dual Y-axis plot combining an area chart (mapped to the right Y-axis)
with scatter points (mapped to the left Y-axis). Supports optional phase
annotations with colored rectangles and labels. Useful for visualizing
time-series data with different measurement scales, such as rates alongside
percentages.
}
\details{
The dual Y-axis is implemented by scaling the area data to fit the left Y-axis
range, then using \code{sec_axis()} to display the original scale on the right.
The scaling factor is calculated as \code{left_range[2] / right_range[2]}.

For best results, provide explicit \code{left_range} and \code{right_range}
to ensure clean axis labels.

The \code{phases} data frame, if provided, must contain columns \code{start},
\code{end}, and \code{label}. Optional columns \code{fill_color} and
\code{border_color} control the appearance of phase rectangles.
}
\examples{
\dontrun{
library(bioRtools)

df <- data.frame(
  time = 1:100,
  rate_a = runif(100, 1, 5),
  rate_b = runif(100, 2, 6),
  efficiency = runif(100, 20, 90)
)

phases <- data.frame(
  start = c(0, 50),
  end = c(50, 100),
  label = c("Phase A", "Phase B"),
  fill_color = c("#90719f90", "#FDDDA090")
)

p <- plot_dual_axis(
  data = df,
  x = "time",
  area_col = "efficiency",
  scatter_cols = c("rate_a", "rate_b"),
  left_range = c(0, 6),
  right_range = c(0, 100),
  phases = phases,
  left_label = "Rate (mmol/L/day)",
  right_label = "Efficiency (\%)"
)
print(p)
}
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
