% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_prism.R
\name{theme_prism}
\alias{theme_prism}
\title{Prism-inspired ggplot2 Theme with Visible Legend Title}
\usage{
theme_prism(
  palette = "black_and_white",
  base_size = 14,
  base_family = "sans",
  base_fontface = "bold",
  base_line_size = base_size/14,
  base_rect_size = base_size/14,
  axis_text_angle = 0,
  border = FALSE,
  gray_background = FALSE
)
}
\arguments{
\item{palette}{Character string specifying the Prism palette.
See \code{names(ggprism::ggprism_data$themes)} for valid options.
Default is "black_and_white".}

\item{base_size}{Numeric value specifying the base font size in points.
Default is 14.}

\item{base_family}{Character string specifying the base font family.
Default is "sans".}

\item{base_fontface}{Character string specifying the base font face.
Default is "bold".}

\item{base_line_size}{Numeric value specifying the base line width.
Default is \code{base_size / 14}.}

\item{base_rect_size}{Numeric value specifying the base rectangle line width.
Default is \code{base_size / 14}.}

\item{axis_text_angle}{Numeric value specifying the axis text rotation angle.
Must be one of 0, 45, 90, or 270. Default is 0.}

\item{border}{Logical indicating whether to draw a panel border.
Default is FALSE.}

\item{gray_background}{Logical indicating whether to use a \code{theme_gray()}-style
panel background (grey panel, white major grid lines, no panel border)
instead of the default clean Prism look. Default is FALSE.}
}
\value{
A ggplot2 theme object.
}
\description{
A modified version of \code{ggprism::theme_prism()} that preserves the
Prism-style aesthetics but keeps the legend title visible instead of
hiding it.
}
\examples{
library(ggplot2)

ggplot(iris, aes(x = Species, y = Sepal.Length, fill = Species)) +
  geom_boxplot() +
  bioRtools::theme_prism()

# theme_gray()-style grey panel with white grid lines
ggplot(iris, aes(x = Species, y = Sepal.Length, fill = Species)) +
  geom_boxplot() +
  bioRtools::theme_prism(gray_background = TRUE)
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
