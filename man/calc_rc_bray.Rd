% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_rc_bray.R
\name{calc_rc_bray}
\alias{calc_rc_bray}
\title{Calculate Raup-Crick on Bray-Curtis (RCbray)}
\usage{
calc_rc_bray(
  data,
  sample = NULL,
  group_col = "group",
  sample_id = "sample",
  runs = 1000,
  null_model = "independentswap",
  verbose = TRUE
)
}
\arguments{
\item{data}{Community matrix (rows = taxa, cols = samples) or a data.frame
whose first column holds taxon IDs.}

\item{sample}{Optional sample metadata data.frame. When provided with
\code{group_col}, RCbray is computed independently within each group.}

\item{group_col}{Column in \code{sample} holding group labels (default "group").}

\item{sample_id}{Column in \code{sample} matching \code{colnames(data)}
(default "sample").}

\item{runs}{Number of null-model randomizations (default 1000).}

\item{null_model}{Null model for \code{picante::randomizeMatrix}
(default "independentswap").}

\item{verbose}{Print progress (default TRUE).}
}
\value{
Long data.frame: \code{from, to, rc_bray} (plus \code{group} when
  grouped). Pairs use the lower-triangle convention to align with
  \code{calc_beta_nti()}.
}
\description{
Computes RCbray, the compositional null-model axis used (alongside
betaNTI/betaNRI) to infer community-assembly processes (Stegen et al. 2013).
RCbray ranges from -1 to 1; |RCbray| > 0.95 with |betaNTI| <= 2 indicates
dispersal-related processes. Requires NO phylogenetic tree.
}
\details{
RCbray = (fraction of null Bray-Curtis values strictly greater than the
observed value, with the observed value counted in the denominator) - 0.5,
then doubled. Null communities are generated with
\code{picante::randomizeMatrix} (default null model \code{"independentswap"}).
}
\examples{
\dontrun{
otu <- read.delim("asv_table.txt", row.names = 1)
rc <- calc_rc_bray(otu, runs = 1000)
}
}
\references{
Stegen, J. C., et al. (2013). ISME J, 7(11):2069-2079.
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
