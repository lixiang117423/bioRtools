% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_beta_nri.R
\name{calc_beta_nri}
\alias{calc_beta_nri}
\title{Calculate Beta Net Relatedness Index (betaNRI)}
\usage{
calc_beta_nri(
  data,
  tree,
  beta_reps = 999,
  abundance_weighted = TRUE,
  verbose = TRUE,
  sample = NULL,
  sample_id = "sample",
  group_col = "group"
)
}
\arguments{
\item{data}{Community matrix (rows = taxa, cols = samples) or a data.frame
whose first column holds taxon IDs.}

\item{tree}{A phylogenetic tree (\code{ape::phylo}).}

\item{beta_reps}{Number of null-model randomizations (default 999).}

\item{abundance_weighted}{Weight by abundance (default TRUE).}

\item{verbose}{Print progress (default TRUE).}

\item{sample}{Optional sample metadata for per-group computation.}

\item{sample_id}{Column in \code{sample} matching \code{colnames(data)}.}

\item{group_col}{Column in \code{sample} with group labels.}
}
\value{
Long data.frame: \code{from, to, beta_mpd, beta_nri} (plus
  \code{group} when grouped).
}
\description{
betaNRI is the standardized effect size of betaMPD (mean pairwise distance),
the deeper-phylogenetic complement to betaNTI (which uses nearest-taxon
distance, betaMNTD). Used alongside RCbray for the full Stegen (2013)
community-assembly process framework. Requires a phylogenetic tree.
}
\details{
Mirrors [calc_beta_nti()]; the only difference is the underlying phylogenetic
turnover metric: betaNRI uses \code{picante::comdist} (MPD, whole-tree signal),
betaNTI uses \code{picante::comdistnt} (MNTD, terminal-tip signal).
}
\examples{
\dontrun{
library(ape)
tree <- read.tree("tree.nwk")
otu <- read.delim("asv_table.txt", row.names = 1)
res <- calc_beta_nri(otu, tree, beta_reps = 999)
}
}
\references{
Stegen, J. C., et al. (2013). ISME J, 7(11):2069-2079.
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
