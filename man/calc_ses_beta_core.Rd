% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ses_beta_core.R
\name{calc_ses_beta_core}
\alias{calc_ses_beta_core}
\title{Shared standardized-effect-size engine for betaNTI/betaNRI}
\usage{
calc_ses_beta_core(
  data,
  tree,
  beta_reps = 999,
  abundance_weighted = TRUE,
  verbose = TRUE,
  sample = NULL,
  sample_id = "sample",
  group_col = "group",
  dist_fun = c("mntd", "mpd"),
  obs_name = "beta_mntd",
  ses_name = "beta_nti"
)
}
\arguments{
\item{data}{Community matrix (rows = taxa, cols = samples) or a data.frame
whose first column holds taxon IDs.}

\item{tree}{A phylogenetic tree (`ape::phylo`).}

\item{beta_reps}{Number of null model randomizations (default: 999).}

\item{abundance_weighted}{Logical; weight by species abundance (default: TRUE).}

\item{verbose}{Print progress messages (default: TRUE).}

\item{sample}{Optional data frame of sample metadata. Must contain a column
with sample IDs matching \code{colnames(data)}.}

\item{sample_id}{Column name in \code{sample} containing sample IDs that match
\code{colnames(data)}. Default is "sample".}

\item{group_col}{Column name in \code{sample} containing group labels
(default: "group"). When provided with \code{sample}, βNTI is calculated
independently within each group.}

\item{dist_fun}{`"mntd"` (picante::comdistnt, nearest taxon) or
`"mpd"` (picante::comdist, mean pairwise).}

\item{obs_name}{Name for the observed-metric output column.}

\item{ses_name}{Name for the standardized-effect-size output column.}
}
\description{
Internal workhorse for [calc_beta_nti()] (dist_fun = "mntd") and
[calc_beta_nri()] (dist_fun = "mpd"). Handles data preparation, tree
matching, optional per-group computation, the taxa-label null-model
randomization, and long-format output. Not exported.
}
\keyword{internal}
