% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gene_features.R
\name{plot_gene_features}
\alias{plot_gene_features}
\title{Plot Gene Features from Simple Format}
\usage{
plot_gene_features(
  data,
  gene_color = "black",
  base_size = 12,
  feature_colors = c(exon = "#4DAF4A", CDS = "#377EB8"),
  max_genes = NULL
)
}
\arguments{
\item{data}{Path to tab-delimited file with columns: id, type, start, end,
or a data frame with the same structure}

\item{gene_color}{Color for gene lines (default: "black")}

\item{base_size}{Base font size for the plot (default: 12)}

\item{feature_colors}{Named vector for feature type colors.
Names should match feature types (e.g., exon, CDS, UTR).
Default is c(exon = "#4DAF4A", CDS = "#377EB8")}

\item{max_genes}{Maximum number of genes to display (default: NULL for all)}
}
\value{
A ggplot object showing gene structure with features
}
\description{
Visualize gene structure from a simple tab-delimited format containing gene
IDs, feature types, start and end positions. Each gene has a 'gene' row
defining its span, and multiple feature rows (exon, CDS, etc.) showing
features within the gene.
}
\examples{
\dontrun{
library(bioRtools)

# From file
p1 <- plot_gene_features("genes.txt")
print(p1)

# From data frame
p2 <- plot_gene_features(df.gene, max_genes = 20)
print(p2)

# Custom colors
p3 <- plot_gene_features(
  data = df.gene,
  feature_colors = c(exon = "#E41A1C", CDS = "#377EB8")
)
print(p3)
}
}
