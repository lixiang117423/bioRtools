% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_func_fr.R
\name{calc_func_fr}
\alias{calc_func_fr}
\title{Functional redundancy (FR)}
\usage{
calc_func_fr(
  asv_func,
  abundance,
  abundance_weighted = FALSE,
  adj_tax = FALSE,
  taxonomy = NULL,
  adj_tax_by = "Genus",
  tax_cols = c(kingdom = "Kingdom", phylum = "Phylum", class = "Class", order = "Order",
    family = "Family", genus = "Genus", species = "Species"),
  remove_zero = TRUE
)
}
\arguments{
\item{asv_func}{ASV x function 0/1 matrix (from [predict_func()]).}

\item{abundance}{ASV x sample count matrix (same row names as `asv_func`).}

\item{abundance_weighted}{Use abundance-weighted FR (default FALSE).}

\item{adj_tax}{Apply the taxonomic adjustment factor (default FALSE).}

\item{taxonomy}{Taxonomy table (required if `adj_tax = TRUE`); ASV IDs in row
names, rank columns per `tax_cols`.}

\item{adj_tax_by}{Rank for the adjustment factor (default "Genus").}

\item{tax_cols}{Named rank -> column mapping (default SILVA-style).}

\item{remove_zero}{Exclude zero-abundance taxa from each sample's totals
(default TRUE).}
}
\value{
Long data.frame: `sample`, `function`, `fr` (plus `adjustment_factor`
  if `adj_tax = TRUE`).
}
\description{
Computes per-sample, per-function functional redundancy from an ASV x
function assignment matrix (e.g. from [predict_func()]) and a count matrix.
Ported from microeco's `trans_func$cal_func_FR`.
}
\details{
For sample s and function f:
\itemize{
  \item unweighted: \code{(N_taxa_with_f_in_s / N_taxa_in_s) * AF}
  \item weighted:   \code{(sum_abund_with_f_in_s / sum_abund_in_s) * AF}
}
where the optional adjustment factor \code{AF = N_unique_<level>_with_f /
N_unique_<level>_total} corrects for taxonomic dispersion (default AF = 1).
}
\examples{
\dontrun{
res <- predict_func(otu, taxo)
fr  <- calc_func_fr(res$asv_func, otu)
}
}
\references{
Louca et al. (2016); microeco `trans_func$cal_func_FR`.
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
