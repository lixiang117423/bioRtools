% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_faprotax.R
\name{parse_faprotax}
\alias{parse_faprotax}
\title{Parse the bundled FAPROTAX database}
\usage{
parse_faprotax(
  path = system.file("extdata", "FAPROTAX.txt", package = "bioRtools")
)
}
\arguments{
\item{path}{Path to FAPROTAX.txt (default: the bundled copy).}
}
\value{
A list: `func_tax` (named list, function -> character vector of
  `*`-delimited clade expressions), `func_ops` (named list, function ->
  list of `c(op, ref)` with op in add/subtract/intersect), `func_meta`
  (named list of metadata strings), `ver` (database version).
}
\description{
Reads the FAPROTAX `.txt` database (shipped in `inst/extdata/`) into a list
of function-to-clade mappings, composite set-operations, and metadata.
Internal helper for [predict_func()].
}
\keyword{internal}
