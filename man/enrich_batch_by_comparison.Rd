% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_batch.R
\name{enrich_batch_by_comparison}
\alias{enrich_batch_by_comparison}
\title{Run enrichment per comparison on a DEG result table}
\usage{
enrich_batch_by_comparison(
  degs,
  db,
  enrich_fun,
  comparison_col = "comparison",
  gene_col = "gene",
  regulation_col = "regulation",
  keep_regulation = NULL,
  ...
)
}
\arguments{
\item{degs}{Data frame with gene / comparison / regulation columns.}

\item{db}{Annotation database (\code{kegg_db} / \code{go_db}).}

\item{enrich_fun}{Single-comparison enrichment function
(\code{enrich_kegg} or \code{enrich_go}).}

\item{comparison_col}{Column naming each contrast.}

\item{gene_col}{Column holding gene identifiers.}

\item{regulation_col}{Column holding regulation labels.}

\item{keep_regulation}{\code{NULL} to drop rows whose regulation is
\code{"NS"} (default); or a character vector of regulation levels to keep.}

\item{...}{Passed to \code{enrich_fun} for every comparison
(e.g. \code{p_adjust}, \code{min_pathway_size}).}
}
\value{
Combined enrichment data frame with a comparison column (0 rows if
  nothing was enriched).
}
\description{
Used by \code{enrich_kegg()} / \code{enrich_go()} when \code{gene} is a data
frame (e.g. the output of \code{find_degs_deseq2()}). Splits the table by its
comparison column, pulls the gene list for each group (dropping
non-significant rows by default), runs the single-comparison enrichment
function, and row-binds the results with a comparison column. A group that
errors or yields no enriched terms is skipped with a warning.
}
\keyword{internal}
