% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_origin_lines.R
\name{add_origin_lines}
\alias{add_origin_lines}
\title{Add Dashed Reference Lines Through the Origin}
\usage{
add_origin_lines(
  x_intercept = 0,
  y_intercept = 0,
  linetype = "dashed",
  color = NULL,
  alpha = NULL,
  linewidth = NULL,
  ...
)
}
\arguments{
\item{x_intercept}{Numeric position on the x axis for the vertical line
(default: 0).}

\item{y_intercept}{Numeric position on the y axis for the horizontal line
(default: 0).}

\item{linetype}{Character string for line type (default: "dashed").}

\item{color}{Character string for line color. `NULL` (default) inherits the
ggplot2 / theme default, so the lines render exactly like a bare
[ggplot2::geom_vline()] / [ggplot2::geom_hline()].}

\item{alpha}{Numeric transparency between 0 and 1. `NULL` (default)
inherits the ggplot2 default.}

\item{linewidth}{Numeric line width. `NULL` (default) inherits the ggplot2
default.}

\item{...}{Additional arguments passed to both [ggplot2::geom_vline()] and
[ggplot2::geom_hline()].}
}
\value{
A list of two ggplot2 layer objects (`geom_vline` then `geom_hline`)
  that can be added to a plot with `+`.
}
\description{
Convenience helper returning a vertical and a horizontal dashed reference
line at (0, 0), the two-line combo commonly used on PCA / PCoA / RDA / t-SNE
score plots. Add it directly with `+`, e.g. `p + add_origin_lines()`.
}
\examples{
\dontrun{
library(ggplot2)

p <- ggplot(iris, aes(Sepal.Length, Sepal.Width)) +
  geom_point() +
  add_origin_lines()
print(p)
}

}
\author{
Xiang LI <lixiang117423@gmail.com>
}
