% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmpr.R
\name{gmpr}
\alias{gmpr}
\title{GMPR size factors (geometric mean of pairwise ratios)}
\usage{
gmpr(comm, intersect_no = 10, ct_min = 1)
}
\arguments{
\item{comm}{Community matrix, taxa x samples (features in rows).}

\item{intersect_no}{Minimum number of shared non-zero taxa required between
a sample pair for it to count toward the geometric mean (default 10).}

\item{ct_min}{Minimum count threshold; counts below this are treated as 0
(default 1).}
}
\value{
Named numeric vector of per-sample size factors (names = sample
  names). Samples that share fewer than `intersect_no` taxa with all peers
  get `NA` (with a warning). `attr(,"nss")` gives the number of qualifying
  peers per sample.
}
\description{
Computes per-sample size factors via the GMPR method (Chen et al. 2018,
<doi:10.7717/peerj.4600>): for each sample, the geometric mean over peers of
the median count ratio on shared non-zero taxa. Ported from microeco's
internal `GMPR`. Internal helper for [normalize_abund()].
}
\keyword{internal}
