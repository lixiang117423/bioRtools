% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cim_lod.R
\name{plot_cim_lod}
\alias{plot_cim_lod}
\title{CIM/QTL LOD Score Plot}
\usage{
plot_cim_lod(
  data,
  threshold = NULL,
  chr_col = "chr",
  pos_col = "pos",
  lod_col = "lod",
  pos_scale = 1e+06,
  lod_offset = 0.1,
  chr_prefix = "Chr",
  title = NULL,
  x_lab = "Chromosome",
  y_lab = "LOD",
  colors = c("grey60", "grey30"),
  threshold_color = "red",
  threshold_linetype = "dashed",
  threshold_linewidth = 0.8,
  y_max = 100,
  y_breaks = seq(0, 100, 20),
  line_size = 0.5
)
}
\arguments{
\item{data}{A data.frame, data.table, or file path (character string).
If a file path, it is read with \code{data.table::fread()}.
Must contain chromosome, position, and LOD score columns.}

\item{threshold}{Numeric value for a genome-wide horizontal threshold
line. Default is NULL (no line).}

\item{chr_col}{Character. Column name for chromosome. Default is "chr".}

\item{pos_col}{Character. Column name for physical position.
Default is "pos".}

\item{lod_col}{Character. Column name for LOD scores. Default is "lod".}

\item{pos_scale}{Numeric. Multiplier applied to the position column to
convert to base pairs. Default is 1e6 for Mbp → bp.
Set to 1 if positions are already in bp.}

\item{lod_offset}{Numeric. Value added to LOD scores to avoid log-scale
issues with zero values. Default is 0.1.}

\item{chr_prefix}{Character. Prefix added to chromosome names for
correct sort order (e.g., "Chr1" > "Chr10" > "Chr2" without padding).
Stripped from display labels. Default is "Chr".}

\item{title}{Character. Plot title. Default is NULL.}

\item{x_lab}{Character. X-axis label. Default is "Chromosome".}

\item{y_lab}{Character. Y-axis label. Default is "LOD".}

\item{colors}{Character vector. Alternating chromosome colors, recycled
across chromosomes. Default is c("grey60", "grey30").}

\item{threshold_color}{Character. Threshold line color. Default is "red".}

\item{threshold_linetype}{Character. Threshold line type.
Default is "dashed".}

\item{threshold_linewidth}{Numeric. Threshold line width.
Default is 0.8.}

\item{y_max}{Numeric. Y-axis upper limit. Default is 100.}

\item{y_breaks}{Numeric vector. Y-axis tick positions.
Default is seq(0, 100, 20).}

\item{line_size}{Numeric. Line width for LOD curves. Default is 0.5.}
}
\value{
A list containing:
\describe{
  \item{plot}{A ggplot object of the LOD score plot.}
  \item{data}{The processed data frame with cumulative positions.}
  \item{chr_centers}{Data frame of chromosome center positions for
    custom axis labeling.}
}
}
\description{
Create a Manhattan-style LOD score plot for Composite Interval Mapping
(CIM) or other QTL scan results. Computes cumulative physical positions
across chromosomes, applies alternating chromosome coloring, and
optionally draws threshold lines.
}
\details{
The function performs the following preprocessing steps:
\itemize{
  \item Adds \code{chr_prefix} to chromosome names for natural sort order
  \item Scales positions by \code{pos_scale}
  \item Filters rows with NA LOD scores
  \item Adds \code{lod_offset} to LOD scores
  \item Computes cumulative positions for genome-wide x-axis
  \item Strips \code{chr_prefix} from display labels
}
}
\examples{
\dontrun{
# Basic usage with a data frame
lod_data <- data.frame(
  chr = rep(1:5, each = 100),
  pos = rep(seq(0, 99), 5) * 1e6,
  lod = abs(rnorm(500, 5, 2))
)
result <- plot_cim_lod(lod_data, threshold = 3.0)
result$plot

# From file
result <- plot_cim_lod("cim_lod_data.tsv",
  threshold = 3.5,
  y_max = 80,
  y_breaks = seq(0, 80, 10)
)
result$plot
}
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
