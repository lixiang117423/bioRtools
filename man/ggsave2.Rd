% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggsave2.R
\name{ggsave2}
\alias{ggsave2}
\title{Save a ggplot with Sensible Defaults}
\usage{
ggsave2(
  filename,
  plot = ggplot2::last_plot(),
  device = NULL,
  path = NULL,
  scale = 1,
  width = 8,
  height = 6,
  units = "in",
  dpi = 600,
  limitsize = TRUE,
  bg = NULL,
  ...
)
}
\arguments{
\item{filename}{File name to create on disk.}

\item{plot}{Plot to save, defaults to last plot displayed.}

\item{device}{Device to use. Can be a function or one of "eps", "ps",
"tex", "pdf", "jpeg", "tiff", "png", "bmp", "svg", or "wmf".}

\item{path}{Path to save plot to (combined with filename).}

\item{scale}{Multiplicative scaling factor (default: 1).}

\item{width}{Plot width in \code{units} (default: 8).}

\item{height}{Plot height in \code{units} (default: 6).}

\item{units}{Units for width and height: "in", "cm", "mm", or "px".}

\item{dpi}{Plot resolution, or "retina"/"print"/"screen".}

\item{limitsize}{When TRUE (default), stops if dimensions exceed 50 inches.}

\item{bg}{Background colour, e.g. "white".}

\item{...}{Other arguments passed to graphics device.}
}
\value{
Invisibly returns the input \code{plot}.
}
\description{
A thin wrapper around \code{\link[ggplot2]{ggsave}} with \code{width = 8}
and \code{height = 6} as defaults. All other parameters are passed through
unchanged and support full autocomplete in RStudio.
}
\examples{
\dontrun{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, disp)) + ggplot2::geom_point()
ggsave2("plot.pdf", p)
}
}
\seealso{
\code{\link[ggplot2]{ggsave}}
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
