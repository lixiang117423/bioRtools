% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rf_taxa_classification.R
\name{rf_taxa_classification}
\alias{rf_taxa_classification}
\title{Random Forest Taxonomic Classification}
\usage{
rf_taxa_classification(
  data,
  taxo,
  sample,
  tax_cols = c(phylum = "phylum", class = "class", order = "order", family = "family",
    genus = "genus", species = "species"),
  feature_col = NULL,
  sample_col = "sample",
  group_col = NULL,
  tax_levels = c("OTU", names(tax_cols)),
  ratio = 0.7,
  seed = 1288,
  seed_tune = 8812,
  num_trees_range = c(1, 25),
  min_node_size_range = c(3, 30),
  term_evals = 10,
  cv_folds = 10,
  top_n = 9,
  fill_palette = "research",
  verbose = TRUE
)
}
\arguments{
\item{data}{ASV/OTU abundance table (features × samples, first column is
feature ID).}

\item{taxo}{Taxonomic classification table (first column is feature ID
matching data).}

\item{sample}{Sample metadata table. Must contain a column matching sample
names in data and a grouping column.}

\item{tax_cols}{Named character vector mapping English level names to actual
column names in the taxo table. Default maps standard English names
(phylum, class, order, family, genus, species).}

\item{feature_col}{Column name for feature IDs in data and taxo.
Default is the first column name.}

\item{sample_col}{Column name for sample IDs in the sample table.
Default is "sample".}

\item{group_col}{Column name for the grouping/response variable in the
sample table.}

\item{tax_levels}{Character vector specifying which taxonomic levels to
analyze. Use "OTU" for feature-level. Default analyzes all levels.}

\item{ratio}{Train/test split ratio. Default is 0.7.}

\item{seed}{Random seed for data splitting. Default is 1288.}

\item{seed_tune}{Random seed for tuning. Default is 8812.}

\item{num_trees_range}{Integer vector of length 2: range for number of
trees search. Default is c(1, 25) (internally multiplied by 20).}

\item{min_node_size_range}{Integer vector of length 2: range for minimum
node size. Default is c(3, 30).}

\item{term_evals}{Number of tuning evaluations. Default is 10.}

\item{cv_folds}{Number of cross-validation folds for tuning. Default is 10.}

\item{top_n}{Number of top important features to display in boxplot.
Default is 9.}

\item{fill_palette}{Fill palette for the top features boxplot.
Default uses \code{scale_fill_research()}.}

\item{verbose}{Logical. Print progress messages. Default is TRUE.}
}
\value{
A list containing:
\describe{
  \item{accuracy}{Data frame of prediction accuracy at each taxonomic level.}
  \item{predictions}{Named list of mlr3 prediction objects per level.}
  \item{importance}{Data frame of variable importance across all levels.}
  \item{plot.accuracy}{ggplot bar chart of accuracy by taxonomic level.}
  \item{plot.top_features}{ggplot boxplot of top N features at OTU level.}
}
}
\description{
Perform random forest classification at multiple taxonomic levels using
\pkg{mlr3}. Aggregates ASV/OTU abundance at each taxonomic level, trains
a tuned random forest model, and returns accuracy, predictions, and
variable importance.
}
\details{
Requires packages: \pkg{mlr3}, \pkg{mlr3learners}, \pkg{mlr3tuning},
\pkg{paradox}, \pkg{ranger}.

The workflow for each taxonomic level:
\enumerate{
  \item Aggregate abundance by sample and taxon
  \item Create wide-format matrix (samples × taxa)
  \item Train random forest with hyperparameter tuning via random search
  \item Evaluate on held-out test set
  \item Extract variable importance
}
}
\examples{
\dontrun{
result <- rf_taxa_classification(
  data = df.asv,
  taxo = df.tax,
  sample = df.sample,
  group_col = "treatment"
)
result$plot.accuracy
result$plot.top_features
result$accuracy
}

}
\author{
Xiang LI <lixiang117423@gmail.com>
}
