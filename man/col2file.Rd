% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col2file.R
\name{col2file}
\alias{col2file}
\title{Write a Data Frame Column to a Plain Text File}
\usage{
col2file(data, col, file, append = TRUE, eol = "\\n")
}
\arguments{
\item{data}{A data frame.}

\item{col}{Column name (string) or index to export.}

\item{file}{Output file path.}

\item{append}{Logical; if TRUE, append to existing file (default: TRUE).}

\item{eol}{End-of-line character (default: "\\n").}
}
\value{
The input \code{data}, invisibly. This makes the function
  pipe-friendly.
}
\description{
Extracts a single column from a data frame and writes it to a text file
with no header and no quoting. Useful for exporting shell scripts,
sample lists, or any single-column data.
}
\examples{
df <- data.frame(cmd = c("bwa mem ref.fa R1.fq R2.fq > sample1.sam",
                         "bwa mem ref.fa R1.fq R2.fq > sample2.sam"))
tf <- tempfile(fileext = ".sh")
col2file(df, "cmd", tf)
readLines(tf)
unlink(tf)
}
\author{
Xiang LI <lixiang117423@gmail.com>
}
